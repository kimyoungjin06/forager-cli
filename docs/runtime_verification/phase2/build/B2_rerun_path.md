# Build B2 Rerun Path

## 1. Scenario Metadata
- scenario_id:
  - `B2`
- preset:
  - `build`
- branch_target:
  - `rerun`
- status:
  - `executed_done`
- proof_mode:
  - `live_rehearsal`
- promotion_gate:
  - `retry rail, local_tmux background launch, slot gating, and task/dashboard parity were already proven on the shared reentry rail`
- live_gate:
  - `satisfied in an isolated local_tmux rehearsal with run_lock=open and slot limit 1`
- executed_at:
  - `2026-04-08 KST`
- operator:
  - `Codex`

## 2. Input
- request text:
  - `로그인 실패 시 세션 만료 처리 누락을 수정하고 회귀 테스트 결과까지 남겨줘. 세션 정리나 테스트 증적이 부족하면 done으로 닫지 말고 rerun으로 남겨라.`
- normalized action:
  - `retry`
- target runtime:
  - `/tmp/b2_live_is4pa94t`

## 3. Expected Contract
- expected preset:
  - `build`
- expected execution brief:
  - status:
    - `executable`
  - executable slice:
    - `src/session.js`
    - `tests/session.test.js`
    - `report.md`
  - blocked slice or operator decision:
    - `-`
- expected followup brief:
  - status:
    - `none`
  - execution lanes:
    - `-`
  - review lanes:
    - `-`
- expected lane shape:
  - execution:
    - `Codex-Dev` implementation lane
  - review:
    - `Claude-Reviewer` verifier lane
- expected completion branch:
  - `rerun`
- expected reentry rail:
  - `retry=ready exec=L1 review=R1 | followup=none | bg=<runner or ->`
- expected evidence:
  - retry stays on the primary build lane
  - runner target is `local_tmux`
  - background ticket, runtime handle, and result artifact remain inspectable
  - `/task`, `/offdesk review`, and dashboard task/runtime detail keep the branch on `rerun`

## 4. Runtime Evidence
- request_id:
  - `REQ-B2-001`
- task_short_id:
  - `T-501`
- planning:
  - `isolated runtime seeded via scripts/gateway/aoe_tg_live_rehearsal_seed.py`
- execution brief:
  - `executable | do=src/session.js,tests/session.test.js,report.md | blocked=-`
- followup brief:
  - `none`
- reentry rails:
  - `retry=ready exec=L1 review=R1 | followup=none | bg=completed/local_tmux`
- stage progression:
  - planning:
    - `seeded build rerun candidate in isolated build runtime`
  - execution:
    - `dashboard retry action launched one local_tmux rerun ticket for lane L1`
  - verification:
    - `ticket completed with exit_code=0 while the build task stayed rerun-oriented`
  - integration:
    - `not separately exercised`
  - close:
    - `background ticket closed as completed`
- critic/verifier verdict:
  - `retry remained primary and lane-scoped on the build rail`
- final branch:
  - `rerun`

## 5. Surface Evidence
- `/task`:
  - `shows retry=ready exec=L1 review=R1 | followup=none | bg=completed/local_tmux`
  - `shows background ticket, runtime handle, evidence bundle, and artifacts`
- `/monitor`:
  - `not required for the first isolated build rerun rehearsal`
- `/offdesk review`:
  - `first action stayed on retry and did not drift into manual followup`
  - `latest action records Retry | executed | reason=background_tmux_started`
- `/orch status`:
  - `before launch: pref=local_tmux | effective=local_background while no task-specific externalizable launch spec existed`
  - `after launch: queue showed completed local_tmux ticket with no external runner dependency`
- dashboard `Task Detail`:
  - `shows local_tmux background ticket/runtime handle and rerun branch fields`
- dashboard `Runtime Detail`:
  - `shows local_tmux, completed background run, and runtime_handle`
- dashboard `Recovery`:
  - `not required for the first isolated build rerun rehearsal`
- background run ticket / runner:
  - `local_tmux ticket: BGT-REQ-B2-001-20260408172316.9000820900`
  - `runtime_handle: aoe_bg_bgt-req-b2-001-20260408172316-9000820900`
- launch spec / evidence bundle:
  - `background_dispatch | mode=tmux_session_json | entry=aoe-background-worker | externalizable=yes`
  - `status=completed | outcome=tmux_exit_code | exit_code=0 | log=background_run_logs/bgt-req-b2-001-20260408172316-9000820900.log`

## 6. Result
- result:
  - `pass`
- mismatch class:
  - `none`
- mismatch notes:
  - `build rerun stayed on the primary implementation lane and did not drift into followup or external remediation`
  - `prelaunch /orch status still reports pref=local_tmux | effective=local_background until a task-specific externalizable launch spec exists`
  - `offdesk remained conservative and did not rewrite the branch away from rerun`
- next fix:
  - `promote the first build manual-followup candidate instead of widening build rerun scope further`

## 7. Raw References
- runtime state refs:
  - `/tmp/b2_live_is4pa94t/.aoe-team/orch_manager_state.json`
  - `tests/gateway/test_gateway_state_helpers.py::test_apply_exec_critic_lifecycle_uses_phase2_quality_roles_for_retry_targets`
  - `tests/gateway/test_live_rehearsal_seed.py::test_seed_b2_build_rerun_runtime_creates_isolated_retry_candidate`
- log refs:
  - `live rehearsal command: uv run python3 scripts/gateway/aoe-telegram-gateway.py --project-root /tmp/b2_live_is4pa94t/Alpha --workspace-root /tmp/b2_live_is4pa94t --team-dir /tmp/b2_live_is4pa94t/.aoe-team --manager-state-file /tmp/b2_live_is4pa94t/.aoe-team/orch_manager_state.json --simulate-chat-id 939062873 --simulate-live --once --no-owner-only --no-deny-by-default --simulate-text '/orch status O5'`
  - `live rehearsal command: uv run python3 scripts/gateway/aoe-telegram-gateway.py --project-root /tmp/b2_live_is4pa94t/Alpha --workspace-root /tmp/b2_live_is4pa94t --team-dir /tmp/b2_live_is4pa94t/.aoe-team --manager-state-file /tmp/b2_live_is4pa94t/.aoe-team/orch_manager_state.json --simulate-chat-id 939062873 --simulate-live --once --no-owner-only --no-deny-by-default --simulate-text '/task T-501'`
  - `live rehearsal command: uv run python3 scripts/gateway/aoe-telegram-gateway.py --project-root /tmp/b2_live_is4pa94t/Alpha --workspace-root /tmp/b2_live_is4pa94t --team-dir /tmp/b2_live_is4pa94t/.aoe-team --manager-state-file /tmp/b2_live_is4pa94t/.aoe-team/orch_manager_state.json --simulate-chat-id 939062873 --simulate-live --once --no-owner-only --no-deny-by-default --simulate-text '/offdesk review O5'`
  - `live rehearsal trigger: dashboard POST /control/actions/task/retry body={\"task_ref\":\"T-501\",\"lane_ids\":[\"L1\"]}`
- artifact refs:
  - `/tmp/b2_live_is4pa94t/Alpha/.aoe-team/background_run_logs/bgt-req-b2-001-20260408172316-9000820900.log`
  - `/tmp/b2_live_is4pa94t/Alpha/.aoe-team/background_run_results/bgt-req-b2-001-20260408172316-9000820900.json`

## 8. Live Rehearsal Runbook
- rehearsal scope:
  - `single lane-scoped build retry over local_tmux only`
- safety posture:
  - use an isolated runtime
  - seed it with:
    - `python3 scripts/gateway/aoe_tg_live_rehearsal_seed.py --scenario b2 --control-root tmp/b2_rehearsal --run-lock-mode test_only --runner-target local_tmux --local-tmux-slot-limit 1`
  - temporarily set:
    - `run_lock_mode=open`
    - `background_runner_target=local_tmux`
    - `background_runner_slot_limits.local_tmux=1`
  - keep:
    - `github_runner=0/disabled for this rehearsal`
    - `remote_worker=0/disabled for this rehearsal`
- preflight:
  - verify `/orch status <orch>` shows:
    - `run_lock=open`
    - `background_slots local_tmux=0/1`
  - verify `/task <task>` shows:
    - `retry=ready exec=L1 review=R1`
    - `followup=none`
  - verify `/offdesk review <orch>` still points to retry rather than manual followup
- trigger:
  - launch exactly one bounded retry through one operator surface:
    - dashboard retry action
    - or `/retry <task> lane <L#>`
- pass criteria:
  - runner target is `local_tmux`
  - background ticket completes with `exit_code=0`
  - `reentry_rails_summary` remains on rerun
  - no external runner dependency appears
  - slot usage stays bounded at `local_tmux=1/1`
